# data-repo — Runtime "brain" (STUB — to be authored)

> Placeholder created during scaffolding. The extraction rules and invariant
> reminders will be written here during "session 2: building the extraction
> brain" (ARD §3). No content yet — do not rely on this file.

This repository is the runtime `PROJECT_ROOT` / `APPROVED_DIRECTORY`:
the single source of truth for departments, processes, meetings, and runs.
Application code lives in the separate `code-repo` (INV-2: code/data separation).

To be written here later:
- Extraction invariants (INV-1..INV-5 from the PRD)
- Pipeline facts the runtime session must always know
- Pointers to `.claude/skills/` and `.claude/agents/`
