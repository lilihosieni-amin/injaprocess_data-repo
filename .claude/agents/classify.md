---
name: classify
description: "STUB — segments the transcript into processes and labels each new / update / unchanged (FR-P3, ARD §5.2). Prompt to be authored in session 2."
model: opus
---

TODO (session 2): author the classification prompt.
Input: transcript path + tagged departments. Output: `runs/{name}/segments.json`.
