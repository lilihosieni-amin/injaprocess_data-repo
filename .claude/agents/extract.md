---
name: extract
description: "STUB — extracts one process segment into an IDEF0/IDEF3 candidate graph or delta (FR-P5, FR-D5, ARD §5.4). Prompt to be authored in session 2."
model: opus
---

TODO (session 2): author the extraction prompt.
Input: one segment (+ existing process.json for updates). Output: candidate graph/delta
with temporary node keys only — never final IDs (INV-1).
Preloads the `idef-extraction` skill.
