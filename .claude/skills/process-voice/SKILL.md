---
name: process-voice
description: "STUB — orchestration playbook for the full extraction pipeline: transcribe → classify → human checkpoint → extract → merge → summarize → commit (ARD §5). To be authored in session 2."
---

TODO (session 2): author the pipeline playbook.
Owns the Telegram checkpoint (FR-P4) and the end-of-run conflict report (FR-M4).
Stage ordering is enforced by CLI precondition gating, not by this playbook (ARD §7).
